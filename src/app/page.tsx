'use client';

import React from 'react';
import ThematicsContainer from '@/components/ThematicsContainer';

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-50">
      <ThematicsContainer />
    </main>
  );
} 